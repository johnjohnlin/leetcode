#include <string>
#include <vector>
#include <cassert>
#include <cstdio>
using namespace std;
struct TreeNode {
    int val;
    TreeNode *left;
    TreeNode *right;
};
struct ListNode {
    int val;
    ListNode *next;
    ListNode(int x) : val(x), next(nullptr) {}
};

int main(int argc, char const* argv[])
{
	Solution s;
	return 0;
}
